export interface FormValues {
    username: string;
    email: string;
    password: string;
    phone: string;

  }
  
  export interface FormProps {
    initialUsername?: string;
    initialEmail?: string;
    initialPassword?: string;
    initialPhone?: string;
  }
  